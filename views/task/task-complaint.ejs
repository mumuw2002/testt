<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        .container {
            width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        .headcomplaint {
            display: flex;
            width: 100%;
            height: 100%;
            align-items: baseline;
        }

        .headcomplaint p {
            margin: 20px 15px;
            font-size: 32px;
            font-weight: 500;
        }

        .headcomplaint .iconback {
            background-color: #5C54E5;
            color: #fff;
            font-size: 20px;
            padding: 6px;
            border-radius: 100%;
        }

        .headcomplaint .iconback:hover {
            background-color: #4842b1;

        }

        .headcomplaint a {
            margin-left: auto;
        }

        .headcomplaint i {
            padding: 15px;
            color: #000;
            font-size: 20px;
            height: 100%;
            border: 1px solid #ccc;
            border-radius: 10px;
        }

        .headcomplaint .iconstatusreport:hover {
            background-color: #eee;
        }


        .form-group {
            margin-bottom: 30px;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        #heading {
            margin-bottom: 15px;
            font-size: 18px;
            font-weight: 500;
        }

        #description,
        #screenshot,
        #additional_info {
            font-size: 14px;
        }

        input[type="text"],
        textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .radio-group {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .radio-group label {
            display: inline-block;
            padding: 5px 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
        }

        .radio-group input[type="radio"] {
            position: absolute;
            clip-path: polygon(0 0, 0 0, 0 0);
        }

        .radio-group input[type="radio"]:checked+label {
            background-color: #eee;
        }

        button[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .image-preview {
            display: flex;
            flex-wrap: wrap;
            margin-top: 10px;
        }

        .image-preview img {
            width: 100px;
            height: 100px;
            object-fit: cover;
            margin-right: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
        }

        .remove-image {
            position: relative;
        }

        .remove-image button {
            position: absolute;
            top: 5px;
            right: 5px;
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            cursor: pointer;
        }

        button[type="submit"].disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/boxicons@latest/css/boxicons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <div class="container">
        <div class="headcomplaint">
            <span><a href="/space" data-bs-toggle="tooltip" data-bs-placement="bottom" title="กลับไปหน้าหลัก"><i
                        data-label="ช่วยเหลือ" class='bx iconback bx-chevron-left'></i></a></span>
            <p>รายงานปัญหา</p>
            <a href="/complaint/statuscomplaint" data-bs-toggle="tooltip" data-bs-placement="bottom"
                title="รายงานปัญหาของฉัน">
                <i class="fa-solid iconstatusreport fa-file-circle-check"></i>
            </a>
        </div>
        <form action="/complaint" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label id="heading" for="category">หมวดหมู่ของรายงานปัญหา<span style="color: red;">*</span></label>
                <p id="category-error" style="color: red;"></p>
                <div class="radio-group">
                    <div class="radio-group">
                        <input type="radio" id="bug" name="category" value="bug" required>
                        <label for="bug">ปัญหาการใช้งาน/ประสบการณ์ผู้ใช้</label>
                        <input type="radio" id="visual" name="category" value="visual">
                        <label for="visual">ปัญหาด้านภาพ/สุนทรียศาสตร์</label>
                        <input type="radio" id="performance" name="category" value="performance">
                        <label for="performance">ปัญหาด้านประสิทธิภาพ</label>
                        <input type="radio" id="crash" name="category" value="crash">
                        <label for="crash">ปัญหาการหยุดทำงาน/ข้อผิดพลาด</label>
                        <input type="radio" id="data" name="category" value="data">
                        <label for="data">ปัญหาด้านข้อมูล/เนื้อหา</label>
                        <input type="radio" id="security" name="category" value="security">
                        <label for="security">ปัญหาด้านความปลอดภัย</label>
                    </div>
                </div>
                <div class="form-group">
                    <label id="heading" for="description">คุณสามารถให้ความกระจ่างเกี่ยวกับปัญหานี้ได้หรือไม่?
                        เพื่อช่วยให้เราเข้าใจปัญหามากขึ้น</label>
                    <textarea id="complaintContent" name="complaintContent" rows="5"
                        placeholder="ให้คำอธิบายโดยละเอียดเกี่ยวกับปัญหา ..." required></textarea>
                </div>
                <div class="form-group screenshot">
                    <label id="heading" for="screenshot">แนบภาพหน้าจอ (ไม่เกิน 3 รูป)</label>
                    <p id="screenshot">แนบภาพหน้าจอ บันทึกหน้าจอ ที่แสดงถึงปัญหา
                        สามารถช่วยให้นักพัฒนาเข้าใจปัญหาได้อย่างมีประสิทธิภาพมากขึ้น</p>
                    <input type="file" id="screenshot" name="screenshot" accept="image/*" multiple
                        onchange="previewImage(this)">
                    <p id="file-count">จำนวนไฟล์ที่เพิ่ม: 0/3</p>
                    <div class="image-preview" id="image-preview"></div>
                </div>

                <div class="form-group">
                    <label id="heading" for="additional_info">ข้อมูลเพิ่มเติม</label>
                    <textarea id="additional_info" name="additional_info" rows="3"
                        placeholder="รวมข้อมูลเพิ่มเติมที่อาจเป็นประโยชน์ ..."></textarea>
                </div>
                <button type="submit">ส่งรายงาน</button>
        </form>
    </div>
    <script>
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl)
        })
    </script>
    <script>
        let selectedFiles = []; // Stores all selected files

        function previewImage(input) {
            const maxFiles = 3;
            const maxFileSize = 5 * 1024 * 1024; // 5MB
            const previewContainer = document.getElementById('image-preview');

            // Check if the total number of files exceeds the limit
            if (selectedFiles.length + input.files.length > maxFiles) {
                alert(`คุณสามารถอัปโหลดได้สูงสุด ${maxFiles} รูปเท่านั้น`);
                input.value = ''; // Clear input
                return;
            }

            for (let i = 0; i < input.files.length; i++) {
                const file = input.files[i];

                // Validate file type
                if (!['image/jpeg', 'image/png', 'image/gif', 'image/jpg'].includes(file.type)) {
                    alert('อนุญาตเฉพาะไฟล์ภาพเท่านั้น (JPEG, PNG, GIF)');
                    return;
                }

                // Validate file size
                if (file.size > maxFileSize) {
                    alert('ไฟล์ต้องมีขนาดไม่เกิน 5 MB');
                    return;
                }

                // Check for duplicate files
                if (selectedFiles.some(f => f.name === file.name)) {
                    alert('ไฟล์นี้ถูกเพิ่มไปแล้ว');
                    return;
                }

                // Add file to selectedFiles array
                selectedFiles.push(file);

                // Create image preview
                const reader = new FileReader();
                reader.onload = function (e) {
                    const imageContainer = document.createElement('div');
                    imageContainer.classList.add('remove-image');

                    const image = document.createElement('img');
                    image.src = e.target.result;
                    imageContainer.appendChild(image);

                    const removeButton = document.createElement('button');
                    removeButton.innerHTML = '&times;';
                    removeButton.onclick = function () {
                        previewContainer.removeChild(imageContainer);
                        selectedFiles = selectedFiles.filter(f => f.name !== file.name);
                        updateFileCount();
                    };

                    imageContainer.appendChild(removeButton);
                    previewContainer.appendChild(imageContainer);
                };
                reader.readAsDataURL(file);
            }
            updateFileCount();
        }

        function updateFileCount() {
            const fileCountDisplay = document.getElementById('file-count');
            fileCountDisplay.textContent = `จำนวนไฟล์ที่เพิ่ม: ${selectedFiles.length}/3`;
        }
    </script>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4"
        crossorigin="anonymous"></script>
</body>

</html>