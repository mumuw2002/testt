<!-- C:\Users\chanya\Desktop\TaskManagement\views\login_register\reset_password.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="/css/loginCss/reset_password.css">
</head>
<body>
    <div class="reset-wrapper">
        <div class="header-wrapper">
            <!-- Back to login icon and link -->
            <a href="/forgot-password" class="">
              <i class='bx bxs-chevron-left-circle bx-flip-vertical' style='color:#7952b3; font-size: 30px;'  ></i><span class="tooltiptext"></span>
            </a>
            <h1 class="reset-title">รีเซ็ตรหัสผ่าน</h1>
          </div>
          <form action="/reset-password" method="POST">
            <% if (error && error.length > 0) { %>
                <div class="alert alert-danger"><%= error.join(', ') %></div>
            <% } %>
            <% if (success && success.length > 0) { %>
                <div class="alert alert-success"><%= success.join(', ') %></div>
            <% } %>
            <div class="inputgroup">
                <label for="newPassword">รหัสผ่านใหม่:</label>
                <input type="password" name="newPassword"  placeholder="รีเซ็ตรหัสผ่าน"required />
            </div>
            <button type="submit">รีเซ็ตรหัสผ่าน</button>
        </form>
        
    </div>
</body>
</html>
